# Copy to .env and set values. For Render, set these in the dashboard instead.
PORT=5000
NODE_ENV=development
JWT_SECRET=your-secret-key-change-in-production

# Optional: use a different path for SQLite (e.g. persistent disk on Render)
# DATABASE_PATH=/opt/render/project/data/database.sqlite
