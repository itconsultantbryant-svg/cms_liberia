# Development — copy to backend/.env
NODE_ENV=development
PORT=5000
JWT_SECRET=dev-only-change-me-to-something-longer-32chars
# CORS_ORIGIN not required locally (localhost defaults apply)
# APP_URL=http://localhost:3004
LOG_LEVEL=debug
# AUTH_DEBUG_RESET=1
