# Frontend (Vercel) — copy to .env.local for local override or set in Vercel Project Settings.
# Never commit real secrets.

# Public backend origin on Render (no trailing slash)
REACT_APP_API_URL=https://your-api.onrender.com
